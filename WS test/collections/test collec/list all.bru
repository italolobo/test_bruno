meta {
  name: list all
  type: http
  seq: 2
}

get {
  url: {{base_url}}/breeds/list/all
  body: none
  auth: inherit
}

tests {
  const { DogListSchema } = require("./_shared/schemas/dog.schema.js");
  const { validateSchemaData } = require("./_shared/helpers/contract.js");
  
  
  test("Contract: Dog list", () => {
    validateSchemaData(DogListSchema, res.body);
    //italo
  });
}

settings {
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
}

meta {
  name: list all
  type: http
  seq: 2
}

get {
  url: {{base_url}}/breeds/list/all
  body: none
  auth: inherit
}

tests {
  const { DogResponseSchema } = require("./_shared/schemas/dog.schema.js");
  const { validateSchema } = require("./_shared/helpers/contract.js");
  
  test("Contract: Dog API", () => {
    validateSchema(DogResponseSchema, res.body);
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
